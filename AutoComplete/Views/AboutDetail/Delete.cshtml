@model AutoComplete.Models.AboutPerson

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>AboutPerson</legend>

    <div class="display-label">AboutDetail</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.AboutDetail)
    </div>

    <div class="display-label">person</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.person.Title)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
